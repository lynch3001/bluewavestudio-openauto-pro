#!/bin/sh
set -e
# Automatically added by dh_installinit/10.10.5
if [ -x "/etc/init.d/ofono" ] && [ "$1" = remove ]; then
	invoke-rc.d ofono stop || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit/10.10.5
if [ -x "/etc/init.d/dundee" ] && [ "$1" = remove ]; then
	invoke-rc.d dundee stop || exit $?
fi
# End automatically added section
